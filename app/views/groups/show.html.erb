<div>
  
  <strong>
  <%= link_to 'new transaction', new_user_expense_path(user_id: current_user.id) %>
  </strong>

  <% @expenses_group.each do |expenses_group| %>

    <%= render expenses_group %>

  <% end %>
</div>